{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build"
  },
  "deploy": {
        "startCommand": "php artisan config:clear && php artisan cache:clear && touch /tmp/database.sqlite && php artisan migrate --force && php -S 0.0.0.0:$PORT -t public"
  }
}
